---
title: "Example Using External Data" 
output: html_document
---

This report shows two versions of the `iris` dataset: an accurate one (included in `drakepkg`)
and an inaccurate one (downloaded from an external source).

```{r content}
library(drakepkg) 
readd(hist)
```

